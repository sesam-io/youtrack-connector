{
  "$id": "/api/pipes/xxxxxx-roles-collect/entity-types/sink",
  "$schema": "http://json-schema.org/schema#",
  "additionalProperties": true,
  "properties": {
    "aliases": {
      "description": "An array of aliases.",
      "items": {
        "additionalProperties": true,
        "properties": {
          "id": {
            "description": "The id of the alias.",
            "type": "string"
          }
        }
      },
      "type": "array"
    },
    "description": {
      "description": "The description of the role.",
      "type": "string"
    },
    "id": {
      "description": "The id of the role.",
      "type": "string"
    },
    "immutable": {
      "description": "A boolean value that indicates if the role is mutable.",
      "type": "string"
    },
    "key": {
      "description": "The key of the role type.",
      "type": "string"
    },
    "name": {
      "description": "The name of the role type.",
      "type": "string"
    },
    "permissions": {
      "description": "An array of permissions that the role has.",
      "items": {
        "additionalProperties": true,
        "properties": {
          "id": {
            "description": "The id of the permission.",
            "type": "string"
          }
        }
      },
      "type": "array"
    }
  },
  "title": "Pipe sink entity type: xxxxxx-roles-collect",
  "type": "object"
}