{
  "_id": "{{@ system @}}",
  "headers": {
    "Authorization": "Bearer $SECRET(api_key)"
  },
  "operations": {
    "groups-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,ringId,name"
      },
      "url": "api/groups"
    },
    "hubprojects-delete": {
      "method": "DELETE",
      "url": "hub/rest/projects/{{ id }}"
    },
    "hubprojects-insert": {
      "method": "POST",
      "url": "hub/rest/projects"
    },
    "hubprojects-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,key,name,archived,description,creationTime,iconUrl,icon,defaultIcon,resources(id),projectRoles(id),transitiveProjectRoles(id),myFavorite,team(id),owner(id),global,organization(id),projectType(id,name)"
      },
      "payload_property": "projects",
      "url": "hub/rest/projects"
    },
    "hubprojects-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,key,name,archived,description,creationTime,iconUrl,icon,defaultIcon,resources(id),projectRoles(id),transitiveProjectRoles(id),myFavorite,team(id),owner(id),global,organization(id),projectType(id,name)"
      },
      "url": "hub/rest/projects/{{ id }}"
    },
    "hubprojects-update": {
      "method": "POST",
      "url": "hub/rest/projects/{{ id }}"
    },
    "issues-delete": {
      "method": "DELETE",
      "url": "api/issues/{{ id }}"
    },
    "issues-insert": {
      "method": "POST",
      "url": "api/issues"
    },
    "issues-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,attachments(id),comments(id),created,description,externalIssue(id),isDraft,links(id),parent(id),project(id),resolved,subtasks(id),reporter(id),updated"
      },
      "url": "api/issues"
    },
    "issues-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,attachments(id),comments(id),created,description,externalIssue(id),isDraft,links(id),parent(id),project(id),resolved,subtasks(id),reporter(id),updated"
      },
      "url": "api/issues/{{ id }}"
    },
    "issues-update": {
      "method": "POST",
      "url": "api/issues/{{ id }}"
    },
    "organizations-delete": {
      "method": "DELETE",
      "url": "hub/rest/organizations/{{ id }}"
    },
    "organizations-insert": {
      "method": "POST",
      "url": "hub/rest/organizations"
    },
    "organizations-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,key,name,description,emailDomain,creationTime,projects(id),ownUsers(id),users(id),allUsers,groups(id),teams(id),projectsCount,iconUrl,icon,defaultIcon,organizationRoles(id)"
      },
      "payload_property": "organizations",
      "url": "hub/rest/organizations"
    },
    "organizations-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,key,name,description,emailDomain,creationTime,projects(id),ownUsers(id),users(id),allUsers,groups(id),teams(id),projectsCount,iconUrl,icon,defaultIcon,organizationRoles(id)"
      },
      "url": "hub/rest/organizations/{{ id }}"
    },
    "organizations-update": {
      "method": "POST",
      "url": "hub/rest/organizations/{{ id }}"
    },
    "projects-delete": {
      "method": "DELETE",
      "url": "api/admin/projects/{{ id }}"
    },
    "projects-insert": {
      "method": "POST",
      "url": "api/admin/projects"
    },
    "projects-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 42,
        "fields": "id,ringId,name,shortName,archived,createdBy(id),description,iconUrl,leader(id),team(id),template"
      },
      "url": "api/admin/projects"
    },
    "projects-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,ringId,name,shortName,archived,createdBy(id),description,iconUrl,leader(id),team(id),template"
      },
      "url": "api/admin/projects/{{ id }}"
    },
    "projects-update": {
      "method": "POST",
      "url": "api/admin/projects/{{ id }}"
    },
    "usergroups-delete": {
      "method": "DELETE",
      "url": "hub/rest/usergroups/{{ id }}"
    },
    "usergroups-insert": {
      "method": "POST",
      "url": "hub/rest/usergroups"
    },
    "usergroups-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "description,iconUrl,autoJoin,autoJoinDomain,requiredTwoFactorAuthentication,parentsRequireTwoFactorAuthentication,users(id),ownUsers(id),userCount,parent(id),subgroups(id),teams(id),organizations(id),project(id),allUsers,registeredUsers,implicit,queriedSingleton,removable,immutable,id,name,projectRoles(id),transitiveProjectRoles(id),sourcedProjectRoles(id),organizationRoles(id),transitiveOrganizationRoles(id),sourcedOrganizationRoles(id)"
      },
      "payload_property": "usergroups",
      "url": "hub/rest/usergroups"
    },
    "usergroups-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "description,iconUrl,autoJoin,autoJoinDomain,requiredTwoFactorAuthentication,parentsRequireTwoFactorAuthentication,users(id),ownUsers(id),userCount,parent(id),subgroups(id),teams(id),organizations(id),project(id),allUsers,registeredUsers,implicit,queriedSingleton,removable,immutable,id,name,projectRoles(id),transitiveProjectRoles(id),sourcedProjectRoles(id),organizationRoles(id),transitiveOrganizationRoles(id),sourcedOrganizationRoles(id)"
      },
      "url": "hub/rest/usergroups/{{ id }}"
    },
    "usergroups-update": {
      "method": "POST",
      "url": "hub/rest/usergroups/{{ id }}"
    },
    "users-delete": {
      "method": "DELETE",
      "url": "hub/rest/users/{{ id }}"
    },
    "users-insert": {
      "method": "POST",
      "url": "hub/rest/users"
    },
    "users-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,login,profile,banned,aliases,name"
      },
      "payload_property": "users",
      "url": "hub/rest/users"
    },
    "users-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,login,profile,banned,aliases,name"
      },
      "url": "hub/rest/users/{{ id }}"
    },
    "users-update": {
      "method": "POST",
      "url": "hub/rest/users/{{ id }}"
    },
    "usersyoutrack-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,login,ringId"
      },
      "url": "api/users"
    },
    "workitems-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 42,
        "fields": "id,author(id),creator(id),text,type(id),created,updated,duration(id,minutes,presentation),date,issue(id),attributes(id,value(id))"
      },
      "url": "api/workItems"
    },
    "roles-delete": {
      "method": "DELETE",
      "url": "hub/rest/roles/{{ id }}"
    },
    "roles-insert": {
      "method": "POST",
      "url": "hub/rest/roles"
    },
    "roles-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,aliases,key,name,description,permissions,immutable"
      },
      "payload_property": "roles",
      "url": "hub/rest/roles"
    },
    "roles-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,aliases,key,name,description,permissions,immutable"
      },
      "url": "hub/rest/roles/{{ id }}"
    },
    "roles-update": {
      "method": "POST",
      "url": "hub/rest/roles/{{ id }}"
    },
    "projectroles-delete": {
      "method": "DELETE",
      "url": "hub/rest/projectroles/{{ id }}"
    },
    "projectroles-insert": {
      "method": "POST",
      "url": "hub/rest/projectroles"
    },
    "projectroles-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{url}}",
      "next_page_termination_strategy": ["empty-result"],
      "params": {
        "$skip": "{% if is_first_page  %}0{% else %}{{previous_params['$top'] * page}}{% endif %}",
        "$top": 50,
        "fields": "id,role(id),project(id),owner(id),teamMember"
      },
      "payload_property": "projectroles",
      "url": "hub/rest/projectroles"
    },
    "projectroles-lookup": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "params": {
        "fields": "id,role(id),project(id),owner(id),teamMember"
      },
      "url": "hub/rest/projectroles/{{ id }}"
    },
    "projectroles-update": {
      "method": "POST",
      "url": "hub/rest/projectroles/{{ id }}"
    }
  },
  "type": "system:rest",
  "url_pattern": "https://{{@ subdomain @}}.youtrack.cloud/%s",
  "verify_ssl": true
}
